import Foundation

// Copy this file to `Secrets.swift` and fill in your real values.
// IMPORTANT: `Secrets.swift` is ignored by git to prevent leaking secrets.

/* EXAMPLE VALUES - Copy these to Secrets.swift
import Foundation

// 📋 SETUP INSTRUCTIONS:
// 1. Copy this file and rename it to "Secrets.swift" (remove the underscore prefix)
// 2. Replace all placeholder values with your actual API keys
// 3. NEVER commit Secrets.swift to version control (it's already in .gitignore)
// 4. Each team member should maintain their own Secrets.swift file

enum Secrets {
    // OpenWeather API Key
    // Get your key at: https://home.openweathermap.org/api_keys
    static let openWeatherAPIKey = "YOUR_OPENWEATHER_API_KEY_HERE"
    
    // Gemini AI API Key
    // Get your key at: https://makersuite.google.com/app/apikey
    static let geminiAPIKey = "YOUR_GEMINI_API_KEY_HERE"

    // Cloudinary Configuration
    // Get your credentials at: https://cloudinary.com/console
    // ⚠️ SECURITY WARNING: NEVER include cloudinaryApiSecret in client apps!
    static let cloudinaryCloudName = "YOUR_CLOUD_NAME_HERE"
    
    // Upload Presets - Configure these as UNSIGNED in Cloudinary Console
    // Go to: Cloudinary Console > Settings > Upload > Upload presets
    // Enable "Unsigned" for both presets
    static let cloudinaryUploadPreset = "post_images"
    static let cloudinaryProductImagesPreset = "product_images"
    
    // 🔒 CLOUDINARY SECURITY NOTE:
    // This template intentionally does NOT include cloudinaryApiSecret
    // Client apps should only use unsigned uploads via upload presets
    // If you need signed uploads, implement a backend API to generate signatures
}
*/

// This file is just for reference and should not be compiled into the app
